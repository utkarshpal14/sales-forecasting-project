import { motion } from 'framer-motion';import { cn } from '../../utils/cn';/** @param {{ children: import('react').ReactNode, className?: string }} props */export const GlassCard=({children,className})=><motion.div whileHover={{scale:1.02}} transition={{type:'spring',stiffness:300}} className={cn('glass rounded-2xl p-5',className)}>{children}</motion.div>;export default GlassCard;