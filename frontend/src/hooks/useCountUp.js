import { useEffect, useState } from 'react';export const useCountUp=(target=0,duration=1500)=>{const [value,setValue]=useState(0);useEffect(()=>{let raf;const start=performance.now();const t=Number(target||0);const step=(n)=>{const p=Math.min((n-start)/duration,1);const eased=1-Math.pow(1-p,3);setValue(t*eased);if(p<1) raf=requestAnimationFrame(step);};raf=requestAnimationFrame(step);return()=>cancelAnimationFrame(raf);},[target,duration]);return value;};export default useCountUp;