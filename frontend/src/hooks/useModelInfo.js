import { useEffect, useState } from 'react';import toast from 'react-hot-toast';import { getModelInfo } from '../api/endpoints';import mockData from '../constants/mockData';export const useModelInfo=()=>{const [data,setData]=useState(null);const [loading,setLoading]=useState(false);const [error,setError]=useState(null);useEffect(()=>{let mounted=true;const execute=async()=>{setLoading(true);setError(null);try{const res=await getModelInfo();if(mounted)setData(res.data);}catch(err){if(mounted){setError(err.message);setData(mockData.modelInfo);toast.error(err.message);}}finally{if(mounted)setLoading(false);}};execute();return()=>{mounted=false;};},[]);return{data,loading,error};};export default useModelInfo;