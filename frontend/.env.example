# Shared (copy to .env for local secrets — gitignored)
REACT_APP_SUPABASE_URL=your_supabase_project_url
REACT_APP_SUPABASE_ANON_KEY=your_supabase_anon_key

# API URL is set per environment (these files are committed):
#   .env.development  → http://localhost:8000        (npm start)
#   .env.production   → https://your-app.onrender.com (Vercel build)
