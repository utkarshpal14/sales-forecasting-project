# Python 3.13+: scikit-learn 1.3.x / numpy 1.24 have no cp313 wheels → pip builds from sdist and needs MSVC.
# Use 1.5.2+ and numpy 2.x so Windows installs from wheels only.
fastapi>=0.104.1
uvicorn>=0.24.0
scikit-learn>=1.5.2,<1.9
pandas>=2.2.0,<3
numpy>=2.0.0,<3
python-multipart>=0.0.6
xgboost>=2.1.0,<4
catboost>=1.2.0,<2
joblib>=1.3.0,<2
